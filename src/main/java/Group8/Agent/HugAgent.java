package Group8.Agent;

/**
 * class HugAgent is an agent configured from G. Aguilar paper which uses the Reciprocal Velocity Obstacle approach of traversing an environment.
 * It basically lets the agent move bitangents of environment.  I.e. agent 'hugs' all the borders of the environment.
 */
public class HugAgent {
    //TODO: create specs based of of Tijn's papers.
}